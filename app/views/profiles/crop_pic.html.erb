<%= form_for @user,:url => profile_path(@user), html: { method: :put, class: 'form-horizontal' } do |f| %>
  <%= f.cropbox :picture, :width => 500 %>
  <%= f.crop_preview :picture, :width => 150 %>
  <%= f.submit 'Save' %>
<% end %>